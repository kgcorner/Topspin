<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>monolythic-packaging</artifactId>
        <groupId>com.kgcorner.topspin</groupId>
        <version>1.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>topspin-server</artifactId>
    <dependencies>
        <!-- Auth Service dependencies -->
        <dependency>
            <groupId>com.kgcorner.topspin</groupId>
            <artifactId>auth-service</artifactId>
            <version>${product.version}</version>
        </dependency>
        <dependency>
            <groupId>com.kgcorner.topspin</groupId>
            <artifactId>auth-service-mongo-port</artifactId>
            <version>${product.version}</version>
        </dependency>

        <!-- Offer Service dependencies -->
        <dependency>
            <groupId>com.kgcorner.topspin</groupId>
            <artifactId>offer-service</artifactId>
            <version>${product.version}</version>
        </dependency>
        <dependency>
            <groupId>com.kgcorner.topspin</groupId>
            <artifactId>offer-service-mysql-port</artifactId>
            <version>${product.version}</version>
        </dependency>
        <dependency>
            <groupId>com.kgcorner.topspin</groupId>
            <artifactId>local-store-service-client</artifactId>
            <version>${product.version}</version>
        </dependency>
        <!-- Store Service dependencies -->
        <dependency>
            <groupId>com.kgcorner.topspin</groupId>
            <artifactId>store-service</artifactId>
            <version>${product.version}</version>
        </dependency>
        <dependency>
            <groupId>com.kgcorner.topspin</groupId>
            <artifactId>store-mongo-port</artifactId>
            <version>${product.version}</version>
        </dependency>

        <!-- User service dependencies -->
        <dependency>
            <groupId>com.kgcorner.topspin</groupId>
            <artifactId>user-service</artifactId>
            <version>${product.version}</version>
        </dependency>
        <dependency>
            <groupId>com.kgcorner.topspin</groupId>
            <artifactId>user-service-mongo-port</artifactId>
            <version>${product.version}</version>
        </dependency>

        <!-- health package -->
        <dependency>
            <groupId>com.kgcorner.topspin</groupId>
            <artifactId>topspin-health-package</artifactId>
            <version>${product.version}</version>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>